# Stable-only options. Anything nightly-gated emits a warning on every
# `cargo fmt` run against the stable toolchain this project targets.
edition = "2024"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
