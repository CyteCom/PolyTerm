[package]
name = "polyterm-ui"
description = "egui shell: tiles, tabs, session tree, terminal and RDP panes."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
polyterm-core = { path = "../polyterm-core" }
polyterm-store = { path = "../polyterm-store" }
polyterm-term = { path = "../polyterm-term" }
eframe = { workspace = true }
egui = { workspace = true }
egui_tiles = { workspace = true }
tokio = { workspace = true }
bytes = { workspace = true }
tracing = { workspace = true }
jiff = { workspace = true }
# Persisting the tile layout (FR-95) through eframe's storage.
serde = { workspace = true }

[dev-dependencies]
# Round-trip test for the persisted layout.
serde_json = { workspace = true }

[lints]
workspace = true
